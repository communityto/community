<main class="site-content">
  <h1>Categories</h1>

  <div class="category">
    <% @categories.each do |category| %>
      <%= link_to category do %>
        <div class="category-name">
          <%= category.name %>
        </div>
      <% end %>
    <% end %>
  </div>
</main>
