<section class="container">
  <p><%= personal_message.body %></p>
  <p>at <strong><%= personal_message.created_at.in_time_zone("Eastern Time (US & Canada)").strftime("%-d %b %Y at %l:%M %P") %></strong><br>
    from <strong><%= personal_message.user.first_name %></strong></p>
  <hr>
</section>
